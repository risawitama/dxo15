allow hal_audio_default sysfs:dir { open read};

get_prop(hal_audio_default, default_prop)
set_prop(hal_audio_default, default_prop)

get_prop(hal_audio_default, vendor_default_prop)
set_prop(hal_audio_default, vendor_default_prop)
